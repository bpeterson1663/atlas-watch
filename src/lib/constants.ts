/** Max event cards shown on the dashboard list. */
export const DASHBOARD_EVENT_LIMIT = 10

/** Max markers rendered on the dashboard read-only map. */
export const DASHBOARD_MAP_LIMIT = 100

/** Default page size for the Event Explorer table. */
export const EXPLORER_PAGE_SIZE = 50

/** Max content width for dashboard and explorer layouts. */
export const PAGE_MAX_WIDTH = 1400
